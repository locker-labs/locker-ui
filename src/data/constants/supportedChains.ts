import { arbitrum, avalanche, optimism, polygon, sepolia } from "wagmi/chains";

export const supportedChains = {
	ARBITRUM: arbitrum,
	OPTIMISM: optimism,
	POLYGON: polygon,
	AVALANCHE: avalanche,
	SEPOLIA: sepolia,
};
